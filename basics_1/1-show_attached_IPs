#!/usr/bin/env bash
# Script to display all active IPv4 IPs on the machine

# Use the `ifconfig` command to get IPv4 addresses and filter out the loopback address
ifconfig | grep -oP '(?<=inet\s)\d+\.\d+\.\d+\.\d+' | grep -v '127.0.0.1'
