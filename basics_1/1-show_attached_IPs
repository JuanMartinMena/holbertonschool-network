#!/usr/bin/env bash
# Script to display all active IPv4 IPs on the machine

# Use the `ip` command to get IPv4 addresses and filter out the loopback address
ip -4 addr show | grep -oP '(?<=inet\s)\d+\.\d+\.\d+\.\d+' | grep -v '127.0.0.1'
