#!/usr/bin/env bash
# Script to display all active IPv4 IPs on the machine

# Use the `ip` command to get active IPv4 addresses and filter out non-IPv4 addresses and loopback addresses
ip -4 addr show | grep -oP '(?<=inet\s)\d+\.\d+\.\d+\.\d+' | grep -vE '^127\.|^::1'
